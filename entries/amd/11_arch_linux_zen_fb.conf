# path:   /home/klassiker/.local/share/repos/efistub/entries/amd/11_arch_linux_zen_fb.conf
# author: klassiker [mrdotx]
# github: https://github.com/mrdotx/efistub
# date:   2022-02-14T16:57:20+0100

# lenovo uefi does not support the same loader (workaround)
cp -u /boot/vmlinuz-linux-zen /boot/vmlinuz-linux-zen-fb

# helper
package_version="$( \
    pacman -Q --color never "linux-zen" \
        | cut -d ' ' -f2 \
        | cut -d '.' -f-3 \
        | cut -d '-' -f1 \
)"

# efibootmgr options
label="Arch Linux $package_version ZEN Fallback"
disk="/dev/nvme0n1"
loader="/vmlinuz-linux-zen-fb"

options="
    initrd=/amd-ucode.img
    initrd=/initramfs-linux-zen-fallback.img
    root=UUID=eac98ebd-4d98-4276-88d1-bbaf9130c2e0 rw
"

# unset helper
unset package_version
